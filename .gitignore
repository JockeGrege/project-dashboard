# tooling
.claude/
.DS_Store

# working docs (kept local)
project-improvements-spec.md
project-dashboard-handoff.md
PROJECT_MAINTENANCE_GUIDE.md

# dependencies
node_modules/

# cloudflare worker (worker/ has its own .gitignore too)
worker/node_modules/
worker/.wrangler/
worker/.dev.vars

# build output
dist/
dist-ssr/
*.tsbuildinfo

# local env
.env
.env.local
.env.*.local

# generated from firestore.rules.template by scripts/gen-firestore-rules.mjs;
# holds the real VITE_ALLOWED_UID and must not be committed to the public repo
firestore.rules

# test / tooling caches
coverage/
playwright-report/
test-results/
.vite/
*.log
firebase-debug.log
firestore-debug.log
ui-debug.log
